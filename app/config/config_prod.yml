imports:
    - { resource: config.yml }

monolog:
    handlers:
        syslog:
            ident: %app_name%
            type: syslog
            level: info
            channels: app
            formatter: syrup.monolog.json_formatter
